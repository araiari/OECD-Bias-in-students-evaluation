# Questa cartella contiene l'Elastic net finalizzata a selezionare le variabili interessanti relative agli studenti.

# I file .jags contengono il modello e tra loro differiscono nella scelta degli iperparametri a1 e a2.
# In elastic_net i parametri seguono una distribuzione esponenziale di parametro fissato.
# In elastic_net i parametri seguono una distribuzione esponenziale di parametro con distribuzione gamma.
# In elastic_net_plus i parametri seguono una distribuzione esponenziale di parametro fissato con supporto (0.5, +inf).

# Sono presenti i workspace dei risultati.
# Il risultato migliore è risultato_EN_a_plus
